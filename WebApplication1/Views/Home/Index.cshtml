@{
    ViewData["Title"] = "品渴茶鋪 - 首頁";
}

<!-- 滿版背景首頁 -->
<div class="hero-section">
    <!-- 品牌LOGO -->
    <div class="brand-logo">
        <img src="~/images/logo.png" alt="品渴茶鋪" class="logo-image" />
    </div>
    
    <div class="hero-content">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8 text-center">
                    <h1 class="hero-title">品渴茶鋪</h1>
                    <div class="flip-text-container">
                        <div class="flip-text active">品渴茶鋪</div>
                        <div class="flip-text">PINK</div>
                        <div class="flip-text">TEAS</div>
                        <div class="flip-text">SHOP</div>
                    </div>
                    <p class="hero-subtitle">鮮花罐罐烤奶專家<br>古法秘製<br>嚴選頂級食材<br>馥郁鮮花芬芳</p>
                    <div class="hero-buttons">
                        <a asp-controller="Home" asp-action="Menu" class="btn btn-primary btn-lg me-3">查看菜單</a>
                        <a asp-controller="Home" asp-action="About" class="btn btn-outline-light btn-lg">了解更多</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- 特色介紹區域 -->
<div class="features-section">
    <div class="container">
        <div class="row">
            <div class="col-md-4 text-center mb-4">
                <div class="feature-card">
                    <i class="fas fa-leaf feature-icon"></i>
                    <h3>優質茶葉</h3>
                    <p>嚴選來自雲南深山的優質蜜香滇紅，確保每一杯都是最佳品質</p>
                </div>
            </div>
            <div class="col-md-4 text-center mb-4">
                <div class="feature-card">
                    <i class="fas fa-coffee feature-icon"></i>
                    <h3>專業煮製</h3>
                    <p>職人手工煮茶，完美茶湯與鮮奶比例，掌握最佳水溫與時間</p>
                </div>
            </div>
            <div class="col-md-4 text-center mb-4">
                <div class="feature-card">
                    <i class="fas fa-heart feature-icon"></i>
                    <h3>頂級鮮花</h3>
                    <p>來自世界各地最頂級的食用級鮮花，每一杯都是芳香馥郁的味覺饗宴</p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // 導航欄滾動效果
        window.addEventListener('scroll', function() {
            const navbar = document.getElementById('mainNavbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // 平滑滾動
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // 文字翻頁動畫
        document.addEventListener('DOMContentLoaded', function() {
            const flipTexts = document.querySelectorAll('.flip-text');
            
            if (flipTexts.length === 0) {
                return;
            }
            
            let currentIndex = 0;
            
            function flipToNext() {
                // 移除当前active类
                flipTexts[currentIndex].classList.remove('active');
                flipTexts[currentIndex].classList.add('prev');
                
                // 移动到下一个
                currentIndex = (currentIndex + 1) % flipTexts.length;
                
                // 添加新的active类
                setTimeout(() => {
                    flipTexts[currentIndex].classList.remove('prev');
                    flipTexts[currentIndex].classList.add('active');
                }, 100);
            }
            
            // 每3秒切换一次
            setInterval(flipToNext, 3000);
        });
    </script>
}